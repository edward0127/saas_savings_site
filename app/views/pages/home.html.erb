<%= render "shared/hero" %>

<section class="mx-auto w-full max-w-7xl px-4 py-14 sm:px-6 lg:px-8">
  <div class="mb-8">
    <p class="text-xs font-semibold uppercase tracking-wider text-cyan-700">What we build</p>
    <h2 class="mt-2 font-heading text-3xl font-bold text-slate-900">Focused digital tools that solve real workflow problems</h2>
  </div>
  <div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
    <% @build_examples.each do |example| %>
      <%= render "shared/feature_card", icon: example[:icon], title: example[:title], description: example[:description] %>
    <% end %>
  </div>
</section>

<section class="mx-auto w-full max-w-7xl px-4 py-14 sm:px-6 lg:px-8">
  <div class="mb-8">
    <p class="text-xs font-semibold uppercase tracking-wider text-cyan-700">How it works</p>
    <h2 class="mt-2 font-heading text-3xl font-bold text-slate-900">A simple path from idea to launch</h2>
  </div>
  <div class="grid gap-4 md:grid-cols-5">
    <% @process_steps.each_with_index do |step, index| %>
      <div class="rounded-2xl border border-slate-200 bg-white/90 p-4 shadow-sm">
        <p class="text-xs font-semibold uppercase tracking-wider text-cyan-700">Step <%= index + 1 %></p>
        <p class="mt-2 text-base font-semibold text-slate-900"><%= step[:title] %></p>
        <p class="mt-1 text-sm text-slate-600"><%= step[:detail] %></p>
      </div>
    <% end %>
  </div>
</section>

<section class="mx-auto w-full max-w-7xl px-4 py-10 sm:px-6 lg:px-8">
  <div class="rounded-2xl border border-emerald-200 bg-emerald-50/80 p-6 shadow-sm">
    <p class="text-xs font-semibold uppercase tracking-wider text-emerald-800">Community Support</p>
    <h2 class="mt-2 font-heading text-2xl font-bold text-slate-900">Special support for charities and non-profits</h2>
    <p class="mt-3 max-w-4xl text-sm leading-6 text-slate-700">
      We want AI productivity gains to benefit mission-driven organisations too. We reserve capacity for pro-bono or heavily discounted projects for verified charities and non-profits. Availability depends on scope, urgency, and current schedule.
    </p>
    <div class="mt-4">
      <%= link_to "View Engagement Options", pricing_path, class: "text-sm font-semibold text-emerald-800 hover:text-emerald-900" %>
      <span class="mx-2 text-emerald-700">|</span>
      <%= link_to "Read FAQs", faq_path, class: "text-sm font-semibold text-emerald-800 hover:text-emerald-900" %>
    </div>
  </div>
</section>

<section id="audit-form" class="mx-auto w-full max-w-7xl px-4 py-14 sm:px-6 lg:px-8">
  <div class="grid gap-8 rounded-3xl border border-cyan-200 bg-gradient-to-br from-white via-cyan-50 to-emerald-50 p-6 shadow-sm lg:grid-cols-2 lg:p-10">
    <div>
      <p class="text-xs font-semibold uppercase tracking-wider text-cyan-700">Start here</p>
      <h2 class="mt-2 font-heading text-3xl font-bold text-slate-900">Get your free project plan</h2>
      <p class="mt-4 text-sm leading-6 text-slate-700">
        Tell us your goal and current setup. We will reply with a practical plan focused on fast delivery, reliable quality, and competitive cost.
      </p>
      <ul class="mt-6 space-y-3 text-sm text-slate-700">
        <li class="flex items-start gap-2"><span class="mt-1 text-cyan-700"><%= inline_icon(:shield) %></span><span>Plain-language recommendations you can act on</span></li>
        <li class="flex items-start gap-2"><span class="mt-1 text-cyan-700"><%= inline_icon(:chart) %></span><span>Clear scope options and delivery timeline</span></li>
        <li class="flex items-start gap-2"><span class="mt-1 text-cyan-700"><%= inline_icon(:workflow) %></span><span>A build approach tailored to your real workflow</span></li>
      </ul>
      <p class="mt-4 text-xs text-slate-500">No obligation. We only use your details to respond to this enquiry.</p>
    </div>
    <div>
      <%= render "leads/form", lead: @lead %>
    </div>
  </div>
</section>
